export const runtime = 'nodejs';
import { NextRequest, NextResponse } from 'next/server'; import { cfbdSeasonTable } from '@/lib/fetchers/cfbd'; import { stdRows } from '@/lib/merge';  export async function POST(req:NextRequest){try{const body=await req.json(); const {year,division='fbs'}=body as {year:number;division?:'fbs'|'fcs'|'ii'|'iii'}; if(!year) return NextResponse.json({error:'year is required'},{status:400}); const rows=await cfbdSeasonTable(year,division); return NextResponse.json({year,division,rows:stdRows(rows)});}catch(e:any){return NextResponse.json({error:e?.message||'unexpected error'},{status:500});}}