export function moneylineToProb(ml:number){if(!Number.isFinite(ml))return null;if(ml<0)return Math.abs(ml)/(Math.abs(ml)+100);return 100/(ml+100);}function erf(x:number){const sign=x<0?-1:1;x=Math.abs(x);const a1=0.254829592,a2=-0.284496736,a3=1.421413741,a4=-1.453152027,a5=1.061405429,p=0.3275911;const t=1/(1+p*x);const y=1-((((a5*t+a4)*t+a3)*t+a2)*t+a1)*t*Math.exp(-x*x);return sign*y;}export function normCdf(z:number){return 0.5*(1+erf(z/Math.SQRT2));}export function spreadToProb(spread:number,sd=13.45){if(!Number.isFinite(spread))return null;return normCdf(spread/sd);}export type OddsRow={home:string;away:string;spread?:number|null;moneyline_home?:number|null;moneyline_away?:number|null;implied_home?:number|null};
