export type TeamRow={season:number;team:string;[k:string]:any};export type Table=TeamRow[];function normalizeKey(k:string){return k.toLowerCase().trim().replace(/\s+/g,'_').replace(/[^a-z0-9_]/g,'');}export function stdRows(rows:any[],opts?:{teamKey?:string,seasonKey?:string}):Table{const tk=opts?.teamKey||'team',sk=opts?.seasonKey||'season';return rows.map(r=>{const out:any={};for(const [k,v]of Object.entries(r))out[normalizeKey(k)]=v;out.team=String(out[normalizeKey(tk)]).trim();out.season=Number(out[normalizeKey(sk)]);return out;});}
